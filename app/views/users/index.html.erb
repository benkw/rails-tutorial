<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %> <%# not a render on a string with name of a partial %>
  <%# actually calling render on a user variable of class User %>
</ul>

<%= will_paginate %>